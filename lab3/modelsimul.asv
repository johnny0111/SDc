load dataset.mat
N = length(yreal);
y = zeros(N);

Ne = length(Ue);
yme = zeros(Ne,1);
kmax = Nex;
Ts = 0.08;

for  index = 4 : Ne 
    
    y(index) = -theta(1)*y(index-1) - theta(2)*y(index-2) - theta(3)*y(index-3)...
        + theta(4)*dctr(index-2) + theta(5)*dctr(index-3);

end

figure
plot((1:kmax)*Ts, Ye,'k',(2:kmax)*Ts,yme,'b')
legend('Real,'Simulation')
tittle('Estimation dataset: Model vs. REal data')
xlabel({'Time [s]'}, 'Interpreter', 'latex')
ylabel({'Output 